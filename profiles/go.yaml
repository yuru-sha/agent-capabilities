schema: 1
id: go
kind: language
description: Go language skills and the agents used to plan, implement, review, and verify Go work.

skills:
  include:
    - languages/go/**

agents:
  include:
    - planner
    - tdd-implementer
    - reviewer
    - test-reviewer
    - repo-doctor

external_skills:
  - tdd
  - code-review
