schema: 1
id: mysql
kind: database
description: MySQL database skills and the agents used to plan, implement, review, and verify MySQL work.

skills:
  include:
    - databases/mysql/**

agents:
  include:
    - planner
    - tdd-implementer
    - reviewer
    - database-reviewer
    - security-reviewer
    - test-reviewer
    - repo-doctor

external_skills:
  - tdd
  - code-review
